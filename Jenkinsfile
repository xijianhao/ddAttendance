pipeline {
   agent any
   stages {
       stage('github-pull') {
           steps {
            //    git branch: "main",
               url: "https://github.com/xijianhao/ddAttendance"
           }
       }
   }
}